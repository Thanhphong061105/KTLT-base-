#include <iostream>

using namespace std;

int main() {
    int a, b, c, d;
    cin >> a >> b >> c >> d;
    
   

    if ((a + c == b && d >= b) || (b + d == a && c >= a)) {
        cout << "YES";
    } else {
        cout << "NO";
    }

    return 0;
}
