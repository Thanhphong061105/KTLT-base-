#include<bits/stdc++.h>
#include<bitset>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n;
    cin >> n;
    for(int i=0;i<(1 << n);++i){
        bitset<32> a(i);
        cout << a.to_string().substr(32-n) << endl;
    }
    return 0;
}
